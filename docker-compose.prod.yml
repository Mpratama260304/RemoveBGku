x-image: &production-image
  image: ${APP_IMAGE:-mpratama260304/removebgku:latest}
  build: null
  pull_policy: always

services:
  web:
    <<: *production-image
  worker:
    <<: *production-image
  beat:
    <<: *production-image
    profiles: [full]
